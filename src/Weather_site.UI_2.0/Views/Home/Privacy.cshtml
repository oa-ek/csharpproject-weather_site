@{
    ViewData["Title"] = "Privacy Policy";
}

@using Weather_site.Core.Entities
@model List<Weather>
<h1>Weather Data</h1>

@if (Model == null || Model.Count == 0)
{
    <p>No weather data found</p>
}
else
{
    var firstItem = Model[0]; 
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => firstItem.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => firstItem.City)
                </th>
                <th>
                    @Html.DisplayNameFor(model => firstItem.Wind)
                </th>
                <th></th>
                <th>
                    Edit
                </th>
                <th>
                    Delete
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.City)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Wind)
                    </td>
                    <td></td>
                    <td>
                        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    </td>
                    <td>
                        <form asp-action="Delete" enctype="multipart/form-data">
                            <button class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
